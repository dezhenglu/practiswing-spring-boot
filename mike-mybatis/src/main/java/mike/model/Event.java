package mike.model;

public class Event {

    private long id;
    private String name;

    @Override
    public String toString() {
        return "Event{" + "id=" + id + ", name='" + name + '\'' + '}';
    }
}
